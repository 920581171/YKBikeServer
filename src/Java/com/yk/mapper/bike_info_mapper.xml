<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yk.dao.BikeInfoDao">

    <resultMap type="com.yk.pojo.BikeInfo" id="resultMap">
        <!-- 用id属性来映射主键字段 -->
        <id property="id" column="_id"/>
        <!-- 用result属性来映射非主键字段 -->
        <result property="userId" column="user_id"/>
        <result property="bikeId" column="bike_id"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
        <result property="mileage" column="mileage"/>
    </resultMap>

    <parameterMap id="parameterMap" type="com.yk.pojo.BikeInfo">
        <parameter property="userId" resultMap="resultMap"/>
        <parameter property="bikeId" resultMap="resultMap"/>
        <parameter property="latitude" resultMap="resultMap"/>
        <parameter property="longitude" resultMap="resultMap"/>
        <parameter property="mileage" resultMap="resultMap"/>
    </parameterMap>

    <select id="selectOne" resultMap="resultMap">
        select *
        from bike_info
        where ${column} = #{property}
    </select>

    <select id="selectList" resultMap="resultMap">
        select *
        from bike_info
        where ${column} = #{property}
    </select>

    <select id="selectTable" resultMap="resultMap">
        select *
        from bike_info
    </select>

    <insert id="insert" parameterMap="parameterMap">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into
        bike_info(`_id`,user_id,bike_id,latitude,longitude,mileage)
        values
        (null,#{userId},#{bikeId},#{latitude},#{longitude},#{mileage})
    </insert>

    <update id="update" parameterMap="parameterMap">
        update
            bike_info
        set
            user_id   = #{userId},
            latitude  = #{latitude},
            longitude = #{longitude},
            mileage   = #{mileage}
        where
            bike_id = #{bikeId}
    </update>

    <delete id="delete" parameterMap="parameterMap">
        delete from bike_info
        where bike_id = #{bikeId}
    </delete>
</mapper>